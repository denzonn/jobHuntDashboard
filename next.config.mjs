/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                protocol: 'https',
                hostname: 'dwewfdkgtegeunhwuine.supabase.co'
            }
        ]
    }
};

export default nextConfig;
